#!/usr/bin/env bash

[ -z "$1" ] && { echo "Usage: $0 <host>"; exit 1; }

REMOTE=$1
NNN_PROG() { nix-shell -p nnn --run "nnn -p -"; }


mapfile -t FILES < <(NNN_PROG)

for f in "${FILES[@]}"; do
    rsync -avh --progress "$f"  "$REMOTE:~"
done
